{
  "name": "trade-lee",
  "version": "1.0.0",
  "private": true,
  "description": "Trade Lee — serverless functions (Stripe billing). The site itself is static HTML/CSS/JS with no build step.",
  "dependencies": {
    "stripe": "^17.4.0",
    "@supabase/supabase-js": "^2.45.4"
  }
}
